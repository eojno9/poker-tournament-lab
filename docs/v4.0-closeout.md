# v4.0 closeout

## Scope

This document records final local verification for the v4.0 stable off-table
study milestone.

This is a docs-only closeout step. It does not create a local tag, push to
GitHub, push a tag, or change runtime code.

## Baseline

| Item | Result |
| --- | --- |
| Design commit | `58cb4d62bec5403c7f23df9c10927c1d2b2dcd55` |
| Implementation commit | `37ab14c861fd42c811f6d9e0d6ca1b67753f1757` |
| Implementation verdict | `V4_0_IMPLEMENTATION_BUNDLE_READY` |
| Latest completed public release | `v3.9` |
| Candidate tag | `v4.0` |
| Candidate title | `v4.0 - Stable off-table study milestone` |
| Original repo remote | Absent |
| Local `v4.0` tag | Absent |

## Verification Results

| Check | Result |
| --- | --- |
| Typecheck | PASS |
| Test | PASS, core `383`, server `82`, web `119` |
| Build | PASS |
| Smoke | PASS, `7/7` |
| Tracked artifacts | PASS, none |
| package/package-lock diff | PASS, unchanged |
| Mojibake scan on touched public/security/source/doc surfaces | PASS |
| Strict privacy/path scan on touched surfaces | PASS |
| GitHub push | None |
| Tag push | None |

The local PowerShell `npm` shim is blocked by the local execution policy, so the
same npm scripts were executed through `npm.cmd`.

## SECURITY And Mojibake

SECURITY.md now contains readable UTF-8 Korean reporting guidance. It keeps
public-safe disclosure boundaries, avoids private contact details, and directs
sensitive details away from public issue bodies.

The Trainer missing tree-configuration fallback is confirmed as
`제공되지 않음`. A focused regression test locks that copy against mojibake
regression.

No user-facing mojibake blocker was found in the touched public, security,
source, or v4.0 documentation surfaces checked in this closeout step.

## Unified Workflow

The current implementation and smoke coverage verify the stable off-table
workflow:

1. Entry/navigation remains understandable.
2. Database and Browser review remain public-source and read-only.
3. Database-to-Analyze handoff shows transferred context and does not auto-run
   analysis.
4. Analyze input, validation, result states, presets, and recent analyses remain
   usable.
5. Trainer study, mistake review, retry/resolved/dismissed flow, and local
   statistics remain local-only.
6. Reset actions preserve unrelated local data.

No live assistance, OCR, screen capture, overlay, hotkey, watcher, poker-client
integration, server-side user history, account sync, cloud sync, or bundled data
artifact was added.

## State Model

The implementation aligns with the v4.0 state model without introducing a new
global state machine. The verified state categories are:

- idle
- ready
- loading/processing
- success
- empty
- invalid
- not solved
- unavailable
- recoverable error
- completed

State copy remains Korean-first where user-facing, and reset scopes remain
local to their owning flow.

## Local Data Contract

The v4.0 local data contract remains aligned with implementation:

| Area | Key |
| --- | --- |
| Analyze presets | `poker-tournament-lab:analyze-presets:v1` |
| Recent analyses | `poker-tournament-lab:recent-analyses:v1` |
| Trainer recent attempts | `ptl.trainer.recent.v1` |
| Legacy Trainer recent attempts | `ptl.trainer.recentAttempts.v1` |
| Trainer mistakes | `ptl.trainer.mistakes.v1` |
| Trainer filters | `ptl.trainer.filters.v1` |

No key rename, version bump, server persistence, account sync, or cloud
persistence was introduced. Corrupt, partial, unknown, and write/remove-failure
fallback behavior remains covered by the existing unit tests.

## Actions v7 Verification

| Check | Result |
| --- | --- |
| Workflow checkout ref | `actions/checkout@v7` |
| Workflow setup-node ref | `actions/setup-node@v7` |
| Node version | `24` |
| npm cache | `npm` |
| Third-party action addition | None |
| Workflow shape | unchanged verify and smoke jobs |

Official upstream refs were checked:

- `actions/checkout` has `refs/tags/v7` and `refs/tags/v7.0.0` at
  `9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0`.
- `actions/setup-node` has `refs/tags/v7` and `refs/tags/v7.0.0` at
  `820762786026740c76f36085b0efc47a31fe5020`.
- GitHub release metadata reports `v7.0.0` for both actions as non-draft and
  non-prerelease releases.

Verdict: `ACTIONS_V7_VERIFIED`.

## Release Acceptance

The local v4.0 acceptance gate is ready:

- typecheck/test/build/smoke PASS
- unified workflow PASS
- Korean-first/accessibility baseline PASS
- storage contract PASS
- mojibake blocker 0
- SECURITY public-safe PASS
- package/package-lock unchanged
- tracked artifacts 0
- strict privacy/path blocker 0 on checked surfaces
- API/server persistence not added
- safety scope maintained
- README/RELEASE_HISTORY consistent with `v3.9` as latest completed public tag
- official action refs verified
- local `v4.0` tag absent until the release step

Public GitHub Actions, public refs, public README current tag, and public
release status remain pending until the public release bundle.

## Known Issues

- Node SQLite experimental warning remains non-blocking while tests pass.
- GitHub Actions v7 refs are verified locally against official upstream refs,
  but public Actions must still be checked after the public upload.
- Sanitized public history remains intentionally shorter than private
  development history.

## Final Verdict

`V4_0_CLOSEOUT_READY`
