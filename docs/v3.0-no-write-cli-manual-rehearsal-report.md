# v3.0 No-write CLI Manual Invocation Rehearsal Report

## Purpose

This report records a controlled no-write manual invocation rehearsal for the Step 15 HRC import preview entrypoint.

The rehearsal used sanitized in-memory fixtures only. It did not add a `package.json` script, connect `process.argv`, call `process.exit`, connect `console.log`, read or write the DB, write files, generate report JSON, call APIs, change UI/runtime behavior, or connect the product import route.

## Invocation Method

Rehearsal date: 2026-06-23

Method:

- One-off inline Node invocation through `tsx`.
- Imported source functions directly:
  - `runHrcImportPreviewEntrypoint`
  - `renderHrcImportPreviewOutput`
  - `buildHrcImportPreviewRow`
- Used a sanitized in-memory fixture.
- Created no temporary repo file.
- Created no generated artifact JSON.
- Created no `artifacts/hrc-dry-run-reports` folder.

## Fixture Summary

The rehearsal fixture used:

- `previewRows` total: 28
- `IMPORT_CANDIDATE`: 19
- `NEEDS_MANUAL_REVIEW`: 8
- `EXCLUDE`: 1
- Expected `importPreviewAllowed`: 19
- Existing solution rows: no canonical key collision
- Target DB path: safe copied DB placeholder under `<local-backup-root>`
- Production DB path: repo-local production DB placeholder
- Repo root path: placeholder
- Backup root path: placeholder
- `dbSha256Before`: `92BBF8DA75EA34ABF4CBCF8111ADBCD729DA93D7DDE690E14EB52482A8E16461`
- `dbSha256After`: `92BBF8DA75EA34ABF4CBCF8111ADBCD729DA93D7DDE690E14EB52482A8E16461`
- Safety checks: all PASS
- `timestampIso`: fixed sample ISO string
- `branchName`: `v3.0-product-import-design`
- `commitHash`: `11b6cd80a740cb9d4a753ad6121349e7b706c2e4`

The fixture did not use actual local paths, actual usernames, actual emails, raw HRC source folders, or sample-external-hrc-folder paths in public output.

## Normal Dry-run Result

Normal invocation:

- Command: `import:hrc:preview --dry-run --target-db <safe-copied-db-placeholder>`
- `exitCode`: 0
- status: `OK`
- `importPreviewAllowed`: 19
- `writeAllowed`: false
- `dbWriteAllowed`: false
- `reportFileWriteAllowed`: false
- stdout lines: 34
- stderr lines: 0
- forbidden exposure count: 0
- DB SHA256 unchanged: true
- copied DB path guard: `ALLOWED_COPIED_DB_TARGET`

## Negative Case Results

The rehearsal also checked expected rejection paths:

| Case | Expected | Observed |
| --- | --- | --- |
| `--write` | safety rejection | `exitCode 2`, no report, stderr sanitized |
| production DB target | blocked copied DB path | `exitCode 2`, `BLOCKED_PRODUCTION_DB_TARGET` |
| repo-local DB target | blocked copied DB path | `exitCode 2`, `BLOCKED_REPO_LOCAL_TARGET` |
| privacy/path failure fixture | privacy/path failure | `exitCode 3`, `PRIVACY_PATH_FAILED` |

All negative cases kept:

- `writeAllowed`: false
- `dbWriteAllowed`: false
- `reportFileWriteAllowed`: false
- forbidden exposure count: 0

## Stdout/Stderr Sanitization

The rehearsal summary confirmed:

- stdout/stderr contained no raw absolute local path.
- stdout/stderr contained no user token.
- stdout/stderr contained no private email.
- stdout/stderr contained no raw HRC folder path.
- stdout/stderr contained no sample-external-hrc-folder path.

## Safety Confirmation

- `package.json` script added: false
- Direct `process.argv` connection added: false
- `process.exit` used by entrypoint: false
- Direct `console.log` connected by entrypoint: false
- DB read/write: false
- DB copy: false
- Copied DB creation: false
- Report JSON generated: false
- File write by entrypoint: false
- Product import route connected: false
- API/UI/runtime changed: false
- Raw zip accessed/copied/extracted/committed: false
- Generated artifact JSON committed: false
- `artifacts/hrc-dry-run-reports` created: false

## Known Issue

None.

## Next Step

Recommended next work:

- Copied DB rehearsal manifest helper, or
- Optional package script design only after no-write rehearsal approval, or
- v3.0-rc1 readiness review after remaining planned design gates.
