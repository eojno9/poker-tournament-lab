# v3.8 release/tag planning

## Scope

This document records the release/tag planning result for v3.8 after closeout
verification. It is docs-only.

This step does not create a local tag, push to GitHub, push a tag, add a
remote to the original repository, or prepare a public upload folder.

## Candidate Release

| Item | Result |
| --- | --- |
| Candidate tag | `v3.8` |
| Candidate title | `v3.8 - v4 readiness gap closure` |
| Candidate target | Final HEAD produced by the v3.8 closeout/release-planning commit |
| Release scope | Trainer readiness hardening for v4.0 |
| Public upload | Not performed in this step |
| Tag creation | Not performed in this step |

## Release Summary

v3.8 is a readiness release candidate. It closes the practical v4.0 blockers
identified after v3.7:

- Trainer state transition guards
- localStorage key/version/fallback review
- legacy recent-history migration
- accessibility and keyboard baseline
- narrow/mobile layout baseline
- Korean-first copy and public-status wording review
- public-safe SECURITY reporting guidance

The release candidate does not add new dependencies, package changes, server
persistence, API persistence, DB/raw/generated artifact workflows, live
assistance, OCR, screen capture, overlay, hotkeys, live watcher behavior, or
poker client integration.

## Verification Gate

| Check | Result |
| --- | --- |
| Typecheck | PASS |
| Test | PASS, core 383 / server 82 / web 118 |
| Build | PASS |
| Smoke | PASS, 7/7 |
| package.json/package-lock.json | Unchanged |
| Tracked artifacts | None |
| Original repo remote | Absent |
| Local `v3.8` tag | Not created |
| GitHub push/tag push | Not performed |

## Public Release Preparation

Before public upload:

- create the local annotated `v3.8` tag only after explicit release execution
  approval
- prepare a fresh public sanitized upload folder
- copy only the public-safe snapshot content
- keep original local history out of the public upload folder
- verify typecheck/test/build/smoke in the public upload folder
- confirm README and RELEASE_HISTORY status text for v3.8
- confirm remote `v3.8` does not already exist
- use normal push only
- do not use force push
- do not use `git push --tags`

## Known Issues

- Node SQLite experimental warning remains a non-blocking note.
- Public release/upload is intentionally deferred to a separate execution step.

## Final Verdict

`V3_8_RELEASE_TAG_PLANNING_READY`
