# v3.3 npm audit warning triage

## Scope

This document records a read-only triage of current npm audit warnings for v3.3 planning.

This step does not change dependencies, lockfiles, runtime code, API code, UI code, database files, or generated artifacts.

## Safety constraints

- No `npm audit fix`
- No dependency changes
- No `package.json` changes
- No `package-lock.json` changes
- No DB writes
- No raw data access
- No generated artifact commits
- No live assistance, RTA, OCR, screen capture, overlay, hotkey, live watcher, or poker client integration

## Audit command

```bash
npm audit --json
```

## Summary

| Item                           | Result |
| ------------------------------ | ------ |
| Audit warning count            | 3 |
| Direct dependency involved     | `vite`, `vitest` |
| Transitive dependency involved | `esbuild` |
| Runtime dependency impact      | No direct bundled application runtime dependency impact identified |
| Dev dependency impact          | Yes, dev/build/test tooling |
| Package changes required now   | No, not in this docs-only step |
| Application blocker            | No |
| Final verdict                  | `TRIAGE_RECOMMENDED_SEPARATE_STEP` |

## Warning details

### Warning 1

| Field                          | Result |
| ------------------------------ | ------ |
| Package                        | `esbuild` |
| Dependency path                | Transitive dev tooling dependency, including Vite-related tooling paths |
| Direct or transitive           | Transitive |
| Dev or runtime                 | Dev/build tooling |
| Severity                       | Low |
| Affected usage in this project | Build/dev tooling only; no bundled production runtime path identified |
| Exploitability assessment      | Low practical exposure for the current public application state because it depends on local development tooling behavior |
| Fix availability               | Fix available through dependency or lockfile refresh |
| Requires package change        | Yes, in a separate dependency-maintenance step |
| Verdict                        | `TRIAGE_RECOMMENDED_SEPARATE_STEP` |

Notes:

- This step records the warning only.
- No package or lockfile update was performed.

### Warning 2

| Field                          | Result |
| ------------------------------ | ------ |
| Package                        | `vite` |
| Dependency path                | Direct root dev dependency |
| Direct or transitive           | Direct |
| Dev or runtime                 | Dev/build tooling |
| Severity                       | High |
| Affected usage in this project | Local web dev/build tooling; no public runtime server exposure is introduced by this repository state |
| Exploitability assessment      | Relevant to development server behavior, especially on Windows path handling; not an application runtime blocker, but should be fixed in a dedicated dependency update |
| Fix availability               | Fix available through dependency or lockfile refresh |
| Requires package change        | Yes, in a separate dependency-maintenance step |
| Verdict                        | `TRIAGE_RECOMMENDED_SEPARATE_STEP` |

Notes:

- The project should update Vite in a separate maintenance step and then re-run typecheck, tests, build, smoke, and public-safe scans.
- No package or lockfile update was performed here.

### Warning 3

| Field                          | Result |
| ------------------------------ | ------ |
| Package                        | `vitest` |
| Dependency path                | Direct root dev dependency |
| Direct or transitive           | Direct |
| Dev or runtime                 | Dev/test tooling |
| Severity                       | Critical |
| Affected usage in this project | Test runner tooling; project scripts use `vitest run` and do not add a public Vitest UI server |
| Exploitability assessment      | High severity in affected tooling modes, but current project usage is local test execution and not public runtime behavior |
| Fix availability               | Fix available through dependency or lockfile refresh |
| Requires package change        | Yes, in a separate dependency-maintenance step |
| Verdict                        | `TRIAGE_RECOMMENDED_SEPARATE_STEP` |

Notes:

- The severity warrants a dedicated dependency-maintenance step.
- It is not treated as a current OSS/Codex application blocker because it is dev/test tooling and no vulnerable UI server is exposed by the app.
- No package or lockfile update was performed here.

## Application readiness impact

The current warning set is a known non-blocking dependency-maintenance issue for the current public repository readiness state.

The warnings should be triaged and fixed in a separate dependency-maintenance step because fixes require dependency or lockfile changes, and this step is docs-only.

## Recommended action

Triage in a separate dependency-maintenance step.

Recommended follow-up:

- Update the affected dev/build/test tooling.
- Re-run `npm audit --json`.
- Re-run typecheck, tests, build, and smoke tests.
- Re-run public-safe scans before any public history reflection.

## Verification

| Check                         | Result |
| ----------------------------- | ------ |
| Docs-only changes             | PASS |
| `package.json` unchanged      | PASS |
| `package-lock.json` unchanged | PASS |
| API/UI/runtime unchanged      | PASS |
| DB SHA256 unchanged           | PASS |
| No GitHub push                | PASS |
| No tag changes                | PASS |
| Original repo remote absent   | PASS |
| Git status clean after commit | Pending commit |
