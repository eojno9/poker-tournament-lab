# v1.5 Plan - Analysis Enhancement (Closed)

## 1) v1.5 Summary
- v1.5는 새 solver 추가 없이 분석 설명력을 강화한 릴리즈입니다.
- 기존 라우팅 정책은 유지됩니다.
  - exact DB hit -> `HRC_PRECOMPUTED_DB`
  - fallback complete -> `FALLBACK_ICM`
  - incomplete -> `NOT_SOLVED`
- 핵심은 FALLBACK_ICM 결과를 더 이해하기 쉽게 read-only로 설명/비교 표시하는 것입니다.

## 2) v1.4 Stable Baseline
- Stable tag: `v1.4`
- Stable commit: `87fd9c95b8c16d2fa6174e51b7e938b23a093642`
- Backup: `<local-backup-root>\poker-tournament-lab-v1.4-20260601-2224`
- v1.4 stable restore rehearsal: PASS

## 3) Goals and Non-Goals
### Goals
- 결과를 공부하기 쉽게 구조화: 민감도/EV 비교/설명 강화
- 기존 payload 기반 read-only 표시 강화
- 결측값은 추측하지 않고 `제공되지 않음`으로 명시

### Non-Goals
- 새 solver 추가 없음 (Nash/approximate Nash 없음)
- exact-match 정책 변경 없음
- nearest recommendation 없음
- 실시간/온테이블 기능 없음

## 4) Workstreams and Completion

### A. Villain Range Sensitivity Core
- Status: DONE
- Commit: `82fc76ee8d90cb98590d6381cfc953709ddb59c4`
- 내용:
  - `packages/core/src/rangeSensitivity.ts` 순수 함수 추가
  - scenario 비교 요약(best/worst, label, limitations) 구조화
  - "Nash 아님" 설명 포함

### B. Analyze Result Sensitivity UI
- Status: DONE
- Commit: `0c8c2706ec3de1e3479103aa1076e450460b4e5c`
- 내용:
  - Analyze 결과에서 FALLBACK_ICM 전용 민감도 블록 연결
  - HRC/NOT_SOLVED에는 표시하지 않음
  - read-only payload 기반 표시 유지

### C. ChipEV vs ICM EV Comparison
- Status: DONE
- Commit: `d8becd9`
- 내용:
  - FALLBACK_ICM 결과 전용 비교 블록 추가
  - ChipEV 값이 없으면 `제공되지 않음`
  - 새 계산이 아니라 기존 payload 표시임을 명시

### D. Range Preset Comparison UI
- Status: DONE
- Commit: `b02e69a`
- 내용:
  - `fallbackMetadata.villainRanges` 기반 비교 표
  - `position`, `presetName`, `editedByUser`, `callRangePct`, `rangeSource` 표시
  - solver 결과가 아니라 입력/가정 비교임을 명시

### E. Fallback Explanation Enhancement
- Status: DONE
- Commit: `8b2d940cb005449f87680b7c2d4a4f28751651b3`
- 내용:
  - fallback 결과임 / exact HRC match 아님 / Nash 아님을 명확화
  - villain range 가정 기반 ICM EV 평가라는 설명 강화
  - assumptions/limitations/modelVersion/villainRanges 표시 유지
  - EV 결측값 `제공되지 않음` 처리 유지

### F. Plan Baseline Commit
- Status: DONE
- Commit: `e209bc6aacb6c3c85d40c8d7d0c984d8b827e0e4`

## 5) DoD (Definition of Done) Status
- [x] 기존 source 라우팅 정책 유지
- [x] FALLBACK_ICM 결과 설명/비교 블록 강화
- [x] 결측값 추측 금지 (`제공되지 않음` 처리)
- [x] `npm.cmd run test` PASS
- [x] `npm.cmd run build` PASS
- [x] `npm.cmd run test:smoke` PASS
- [x] scope guard 위반 없음

## 6) Forbidden Scope (Reconfirmed)
- Nash solver / approximate Nash
- PKO / bounty / satellite special solver logic
- Postflop solver
- Nearest match recommendation
- Hand history parser
- OCR / screen capture / overlay / hotkey / live watcher
- Poker client integration / real-time assistance / RTA workflow

## 7) Known Issues
- 없음

## 8) Next Step
- v1.5 최종 검증 실행 후 `v1.5-rc1` 태그/백업 단계 진행
