# v3.2 Copied DB Command Planning

## Summary

v3.2 starts from the completed v3.1 stable baseline and plans the next layer above the copied DB rehearsal foundation: command and entrypoint readiness. This step is docs-only. It does not implement an entrypoint, add a package script, write any database, connect API or UI flows, or push to GitHub.

## v3.1 Stable Baseline

- Stable tag: `v3.1`
- v3.1 tag target: `fcf3827a66d490a77587d5b98b6da7f609cf0ed9`
- v3.1 stable restore rehearsal: `STABLE_RESTORE_REHEARSAL_PASS`
- v3.1 stable backup and cleanup-only verification: PASS
- DB SHA256 baseline: `92BBF8DA75EA34ABF4CBCF8111ADBCD729DA93D7DDE690E14EB52482A8E16461`
- v3.1 scope: copied DB rehearsal planning foundation
- Known issue at v3.1 stable: none

## Goals

- Build on the v3.1 copied DB rehearsal foundation to safely plan or prepare the command and entrypoint layer.
- Review a copied DB command entrypoint implementation path without enabling production DB writes.
- Define an explicit approval gate before any package script can be added.
- Separately evaluate whether report JSON write should ever be introduced.
- Make the restore-test based copied DB rehearsal procedure more concrete before any real copied DB write rehearsal.
- Preserve the approval to guard to plan to report to orchestrator to output renderer flow from v3.1.
- Keep command output aligned with the v3.1 safe redacted renderer policy.

## Non-goals

- No production DB write.
- No repo-local DB write.
- No actual product import route connection.
- No API import workflow.
- No UI import workflow.
- No raw HRC zip direct processing.
- No GitHub push.
- No immediate `package.json` import or preview script.
- No immediate report JSON write.
- No production DB schema migration.
- No raw HRC zip copy, extraction, or commit.

## Safety Boundaries

- Original repo remote remains absent.
- GitHub push remains forbidden unless a later step explicitly approves it.
- Actual DB write remains forbidden, including copied DB write, until a later step explicitly approves the exact target and procedure.
- Any command entrypoint must remain disconnected from `package.json` scripts until a separate approved step.
- Any future output must use the v3.1 Step 9 renderer policy and must not expose real local paths, email addresses, private tokens, raw HRC paths, or raw zip paths.
- Copied DB rehearsal remains forbidden outside an approved restore-test or backup-root rehearsal area.
- Production DB targets are rejected even with approval.
- Repo-local DB targets are rejected even with approval.
- Report file write remains disabled until separately planned and approved.
- Product import API/UI surfaces remain disconnected.

## Proposed Step 1

### Copied DB Command Entrypoint Feasibility Review

Review whether the next implementation step should remain docs-only or introduce a no-package-script command entrypoint.

Expected output:

- A clear decision on whether command entrypoint implementation is safe to start.
- A list of required guards for any future entrypoint.
- Confirmation that package script wiring remains a separate approval step.
- Confirmation that report JSON write remains disabled.
- Confirmation that production DB and repo-local DB writes remain impossible.

Status: completed in `docs/v3.2-command-entrypoint-feasibility-review.md`.

Decision: `FEASIBLE_WITH_RESTRICTIONS`.

The review confirms that a future source-level entrypoint can be considered only if it remains disconnected from `package.json`, defaults to dry-run behavior, rejects production and repo-local DB targets, uses the v3.1 helper chain, writes no report file, and exposes only safe redacted output.

## Step 1 Addendum

### GitHub Clean Snapshot Encoding Audit Docs-only

Review the risk of Korean text mojibake in GitHub/private clean snapshots before any future upload.

Status: completed in `docs/v3.2-github-clean-snapshot-encoding-audit.md`.

Local tracked text audit result:

- audited tracked `*.md`, `*.txt`, and `*.json` files, excluding `package-lock.json`: 77
- mojibake pattern hits: 0
- strict UTF-8 read failures: 0
- UTF-8 BOM files: 0
- UTF-8 without BOM files: 77

The live GitHub private repo was not modified, no remote was added, and no push was performed.

## Step 2

### GitHub Clean Snapshot Encoding Fix Plan Docs-only

Define the future clean snapshot encoding policy after the user observed broken Korean text in a GitHub/private snapshot.

Status: completed in `docs/v3.2-github-clean-snapshot-encoding-fix-plan.md`.

Decision: `PLAN_READY_FOR_FUTURE_CLEAN_SNAPSHOT_ENCODING_FIX`.

The plan keeps the current tracked repo text baseline as clean and moves the fix gate to future clean snapshot creation: strict UTF-8 validation, UTF-8 without BOM preference, explicit PowerShell encoding, post-zip/extract validation, mojibake scan, and privacy/path scan before any GitHub upload.

### Sensitive Git History Scan Read-only

Run a read-only scan across the working tree, HEAD tracked files, reachable Git commits, tags, history filenames, and commit diffs.

Status: completed in `docs/v3.2-sensitive-history-scan-report.md`.

Decision: `CLEAN_SNAPSHOT_REQUIRED_FOR_GITHUB_UPLOAD`.

Summary:

- Current HEAD is clean for targeted real private relay, user, and local path literals.
- Reachable Git history still contains older local path/user/private relay fixture hits.
- No actual API key, GitHub token, OpenAI key, bearer token, DB, zip, raw HRC zip, generated dry-run JSON, `node_modules`, or `apps/web/dist` was found in tracked history.
- Full local Git history push remains high risk because old commits and tags preserve sensitive local history.
- Sanitized clean snapshot upload remains the recommended GitHub path.

## Step 3

### GitHub Clean Snapshot Regeneration Procedure And Public History Plan Docs-only

Document the procedure for rebuilding a sanitized GitHub upload snapshot from current HEAD without Korean text mojibake and without old Git history. Also document the GitHub snapshot issues and public history plan.

Status: completed in `docs/v3.2-github-clean-snapshot-regeneration-procedure.md`.

Additional public history plan: completed in `docs/v3.2-public-history-plan.md`.

Decision: `PROCEDURE_READY_FOR_FUTURE_CLEAN_SNAPSHOT_REGENERATION`.

Public history recommendation: `RECOMMEND_CANDIDATE_B`.

GitHub snapshot issues recorded:

- one visible commit
- `README.md` Korean mojibake observed
- root `LICENSE` file absent

Summary:

- Clean snapshot must be created outside the original repo.
- Include only current HEAD approved source/docs/tests/config/package files.
- Exclude `.git`, old history/tags, local backup/restore-test folders, build outputs, sidecars, raw HRC zip files, generated artifacts, and nested zips.
- Validate `*.md`, `*.txt`, and `*.json` as strict UTF-8, preferably without BOM.
- Run mojibake, privacy/path, actual secret regex, raw artifact, and generated artifact gates before upload.
- If initialized as a new Git repo, do not attach the original repo remote and do not import private old history.
- To reduce the one-commit public repo problem, prefer sanitized stable snapshot public history: v2.9 to v3.0 to v3.1 to v3.2 snapshot commits.
- Add LICENSE in a separate approved step before upload; MIT is the current candidate.
- Fix/validate README encoding in a separate approved step before upload.
- GitHub push/upload remains forbidden until a separate approved step.

## Step 4

### LICENSE Addition

Add the root license file required before a GitHub clean snapshot upload.

Status: completed in `LICENSE`.

License: MIT.

Copyright holder: `eojno9`.

Summary:

- Root `LICENSE` file added.
- GitHub upload precondition for a visible license is now satisfied.
- Full local Git history push remains forbidden.
- Sanitized stable snapshot public history remains the recommended GitHub path.
- README encoding fix/verification remains a separate next step.
- No package script, API/UI/runtime, DB, or raw HRC artifact changes were made.

## Step 5

### README Encoding Fix And Verification

Verify the current `README.md` as the source for future clean snapshot regeneration.

Status: completed.

Result: `README_SOURCE_ENCODING_PASS`.

Summary:

- `README.md` strict UTF-8 validation: PASS.
- UTF-8 BOM: absent.
- mojibake scan: 0 hits.
- broken-Hangul suspicious pattern scan: 0 hits.
- targeted README privacy/path scan: 0 hits.
- README first-screen Korean text is readable when decoded as UTF-8.
- No README content change was required in the original repo.
- The observed GitHub snapshot mojibake remains assigned to the clean snapshot generation/upload preparation path.
- GitHub push/upload remains forbidden until a separate approved step.

## Step 6

### GitHub Clean Snapshot Regeneration Dry-run

Create a repo-external clean snapshot dry-run folder from current HEAD tracked files, without GitHub upload and without initializing a clean snapshot Git repo.

Status: completed outside the original repo.

Result: `SNAPSHOT_GENERATION_PASS_UPLOAD_GATE_FAIL`.

Summary:

- Dry-run snapshot folder was created outside the original repo.
- Copied file count matched current HEAD tracked files: 220.
- Required files such as `README.md`, `LICENSE`, `package.json`, `docs/`, `apps/`, and `packages/` were present.
- Excluded artifact scan passed for `.git`, `node_modules`, dist output, DB sidecars, nested zip files, raw HRC zip files, generated dry-run JSON, and `artifacts/hrc-dry-run-reports`.
- `README.md` strict UTF-8 validation passed.
- README mojibake scan passed with 0 hits.
- `*.md`, `*.txt`, and `*.json` strict UTF-8 validation passed with 84 files and 0 invalid files.
- BOM scan passed with 0 BOM files.
- Actual secret/API key/token was not found after excluding audit/policy documentation self-hits.
- DB was not included because the dry-run copied current HEAD tracked files only and the DB body is not tracked.
- Clean snapshot Git init was not performed.
- GitHub push/upload was not performed.

Upload gate failure:

- The full tracked-file snapshot included v3.2 audit/policy docs that intentionally contain scan-target literals.
- Those internal docs caused privacy/security self-hits for private relay, local username/path, and a documented secret-regex label.
- Excluding audit/policy docs reduced targeted privacy/path hits and actual secret regex hits to 0.

Conclusion:

- The snapshot generation mechanics and encoding gates are good.
- A clean snapshot upload allowlist/exclusion policy is required before the next upload dry-run.

## Step 7

### GitHub Clean Snapshot Upload Allowlist Policy Docs-only

Define which files can be included in a public-safe GitHub clean snapshot and which internal audit/policy docs must be excluded or replaced with redacted public-safe variants.

Status: completed in `docs/v3.2-github-clean-snapshot-upload-allowlist-policy.md`.

Decision: `UPLOAD_ALLOWLIST_POLICY_READY`.

Summary:

- GitHub upload snapshots must be public-safe source snapshots, not internal audit bundles.
- Current HEAD remains the source baseline, but tracked files are not all copied by default.
- Include `README.md`, `LICENSE`, package metadata, source, tests, config, and public-safe docs.
- Exclude internal v3.2 audit/history/security/upload policy docs that intentionally include scan-target literals.
- Redacted public-safe variants may be created later for release, security, privacy, or public history summaries.
- Upload gates must pass after allowlist/exclusion is applied: privacy/path 0, actual secret regex 0, mojibake 0 outside allowlisted pattern docs, UTF-8 invalid 0, BOM 0 preferred, forbidden artifacts 0, and old Git history absent.
- GitHub push/upload remains forbidden until a separate approved step.

## Step 8

### GitHub Clean Snapshot Regeneration Dry-run v2

Regenerate a repo-external clean snapshot dry-run folder with the Step 7 upload allowlist/exclusion policy applied.

Status: completed outside the original repo.

Result: `UPLOAD_GATE_PASS_FOR_DRY_RUN_V2`.

Summary:

- Copied file count: 212.
- Included top-level items: `README.md`, `LICENSE`, package metadata, `apps/`, `packages/`, `scripts/`, `tests/`, root config, and public-safe docs.
- Internal v3.2 audit/security/upload policy docs were excluded.
- Public-safe docs included: 63.
- `README.md` strict UTF-8 validation passed.
- README mojibake scan passed with 0 hits.
- `*.md`, `*.txt`, and `*.json` strict UTF-8 validation passed with 76 files and 0 invalid files.
- BOM scan passed with 0 BOM files.
- Privacy/path scan passed with 0 targeted hits.
- Actual secret/API key/token regex scan passed with 0 hits.
- Artifact scan passed with no `.git`, `node_modules`, dist output, DB sidecars, nested zip, raw HRC zip, generated dry-run JSON, or `artifacts/hrc-dry-run-reports`.
- `LICENSE` and `package.json` were included.
- DB body was not included under the current tracked-source snapshot policy.
- Clean snapshot Git init was not performed.
- GitHub push/upload was not performed.

Conclusion:

- The allowlist/exclusion policy fixed the Step 6 self-hit problem.
- The dry-run v2 folder is an upload-shape candidate, but upload still requires a separate explicit plan and approval.

## Step 9

### Private GitHub Clean Snapshot Upload Plan Docs-only

Define the upload strategy and public history choice before any GitHub upload or clean snapshot Git init.

Status: completed in `docs/v3.2-private-github-clean-snapshot-upload-plan.md`.

Decision: `UPLOAD_PLAN_READY_NO_PUSH`.

Summary:

- Full local Git history push remains forbidden because old reachable commits and tags contain local path/user/private relay fixture traces.
- GitHub upload must use a sanitized clean snapshot only.
- Step 8 dry-run v2 is recorded as `UPLOAD_GATE_PASS_FOR_DRY_RUN_V2`.
- The one-commit GitHub snapshot problem is acknowledged as a project-evidence weakness.
- Candidate A is a single v3.2 clean snapshot upload: safest and simplest, but it keeps the one-commit problem.
- Candidate B is sanitized stable public history: v2.9 to v3.0 to v3.1 to v3.2 snapshot commits, with every snapshot independently verified.
- Recommendation: `PREFER_CANDIDATE_B_WITH_CANDIDATE_A_FALLBACK`.
- Actual upload must not use the original repo remote. It must use a clean upload folder or GitHub web upload after explicit approval.
- GitHub push/upload remains forbidden until a separate approved execution step.

## Step 10

### Sanitized Public History Construction Dry-run

Construct a repo-external dry-run public history with sanitized stable snapshot commits instead of pushing full private history.

Status: completed outside the original repo.

Result: `PUBLIC_HISTORY_DRY_RUN_PASS`.

Strategy: Candidate B, sanitized stable snapshot public history dry-run.

Commit count: 4.

Dry-run public history:

- `88d88ff v2.9: sanitized stable source snapshot`
- `85dc98d v3.0: copied DB import foundation snapshot`
- `1fe38a3 v3.1: copied DB rehearsal planning foundation`
- `d7f4b47 v3.2: clean snapshot upload preparation`

Gate summary:

- privacy/path history scan: 0
- actual secret/API key/token history scan: 0
- mojibake scan: 0
- strict UTF-8 invalid files: 0
- UTF-8 BOM files: 0
- forbidden artifacts: 0
- DB body: not included
- old private Git history: not included
- dry-run repo remote: absent
- GitHub push/upload: not performed
- original repo remote: absent

Notes:

- v2.9 was sanitized in the dry-run repo with placeholder redaction and DB/dist exclusion.
- v3.0 and v3.1 used stable source archives with DB/build artifacts excluded.
- v3.2 used the Step 8 dry-run v2 allowlisted clean snapshot.
- v2.9-v3.1 historical public commits do not include a root `LICENSE`; final v3.2 includes the MIT `LICENSE`.

Conclusion:

- Candidate B is viable as a clean public history shape.
- Upload execution still requires a separate plan and explicit approval.

## Step 11

### Clean Public History Upload Execution Plan Docs-only

Document the actual upload execution plan for the Step 10 sanitized public history candidate without pushing, adding remotes, or mutating the dry-run repo.

Status: completed in `docs/v3.2-clean-public-history-upload-execution-plan.md`.

Decision: `UPLOAD_EXECUTION_PLAN_READY_NO_PUSH`.

Summary:

- The Step 10 dry-run public history repo is the upload candidate.
- Full private/original Git history push remains forbidden.
- The original repo must remain remote-free.
- Recommended upload method: create a separate upload execution folder, then add the GitHub remote only there after explicit approval.
- Existing GitHub repo state must be checked before upload.
- Force push is forbidden unless the user separately approves replacing existing GitHub repo contents.
- Required user approval wording is documented.
- GitHub push/upload remains forbidden until a separate approved execution step.

## Step 12

### GitHub Repo Current State Check Read-only

Check the live GitHub repo state before any upload, without pushing, adding remotes, force pushing, or mutating the upload candidate.

Status: completed in `docs/v3.2-github-repo-current-state-check.md`.

Decision: `GITHUB_STATE_CHECK_COMPLETE_UPLOAD_BLOCKED`.

Summary:

- GitHub repo was accessible read-only through `gh` and `git ls-remote`.
- GitHub repo visibility: public.
- Default branch: `main`.
- Remote HEAD: `91dc8e243d7600fe663824179b48a48877665ec7`.
- Branches: `main` only from read-only ref check.
- Tags: `v2.9` present.
- GitHub commit count estimate: 1.
- Existing history state: one-commit snapshot.
- GitHub README decode confirmed BOM present and mojibake/suspicious broken-Hangul hits.
- Upload candidate has 4 sanitized public history commits and no remote.
- Candidate latest commit: `d7f4b47ce308fc5faa10f78f1b69ef56324fa19e`.
- Current GitHub `main` and candidate public history are unrelated.
- Upload method verdict: `UPLOAD_BLOCKED_NEEDS_USER_DECISION`.
- Replacing current GitHub `main` with the candidate history would likely require force push or explicit replacement.
- Force push remains forbidden unless separately approved.
- Original repo remote remains absent.

Required user decisions before upload:

- Make the GitHub repo private before upload, or explicitly accept public upload.
- Approve replacing the existing one-commit snapshot with the sanitized 4-commit public history.
- If replacement requires force push, explicitly approve force push in the upload execution step.

## Step 13

### GitHub Repo Privacy And Upload Decision Docs-only

Document the visibility and upload-method decision required after Step 12, without changing GitHub visibility, pushing, adding remotes, force pushing, or mutating the upload candidate.

Status: completed in `docs/v3.2-github-repo-privacy-change-decision.md`.

Decision: `VISIBILITY_AND_UPLOAD_DECISION_DOCUMENTED_NO_ACTION`.

Summary:

- Step 12 blocker is confirmed: GitHub repo is public, current repo has a one-commit snapshot, and the upload candidate is unrelated sanitized 4-commit history.
- Visibility choices are documented: keep public or change to private before upload.
- Upload choices are documented: keep the one-commit snapshot or replace it with the sanitized 4-commit public history.
- Force push remains forbidden unless separately and explicitly approved.
- Recommended decision: make the GitHub repo private, create a separate upload execution folder, add the remote only there, rerun final scans, then replace the existing `main` only after explicit approval.
- Required approval text is documented.
- No GitHub visibility change, push, force push, web upload, remote addition, or repo mutation was performed.

## Step 14

### Upload Execution Folder Clone/Copy Dry-run

Create a separate upload execution folder from the Step 10 sanitized public history dry-run repo.

Status: completed outside the original repo.

Decision: `UPLOAD_EXECUTION_DRY_RUN_READY_NO_REMOTE`.

Summary:

- Step 10 public history dry-run repo was copied to a separate upload execution folder.
- The copied repo preserved the 4-commit sanitized public history.
- Commit flow remained v2.9 -> v3.0 -> v3.1 -> v3.2.
- Remote was absent at the end of the dry-run.
- Tags were absent in the upload execution repo.
- Privacy/path, secret/API key/token, encoding, mojibake, artifact, README, LICENSE, and DB-inclusion gates passed.
- GitHub visibility was not changed.
- GitHub push/upload was not performed.
- Original repo remote remained absent.

## Step 15

### Final Pre-push Verification And Approved Upload

Verify the upload execution folder immediately before upload, then replace GitHub `main` after the
user explicitly approved public upload and replacement of the existing GitHub snapshot.

Status: completed outside the original repo.

Decision: `SANITIZED_PUBLIC_HISTORY_UPLOADED_TO_MAIN`.

Summary:

- Final pre-push verification passed for the upload execution folder.
- The user explicitly approved replacing the existing GitHub repo contents with sanitized public history while keeping the repo public.
- Upload was performed from the upload execution folder only.
- Original repo remote remained absent.
- GitHub `main` was replaced with the sanitized 4-commit history.
- New GitHub `main` head: `d7f4b47ce308fc5faa10f78f1b69ef56324fa19e`.
- GitHub README API check after upload showed no BOM and no mojibake.
- Existing remote `v2.9` tag was not changed during the upload.

Known follow-up:

- Remote `v2.9` tag still points to the previous one-commit snapshot and needs a separate cleanup decision.

## Step 16

### Remote Tag Cleanup And Data-Origin Disclosure Scan

Scan public GitHub refs after the `main` upload, especially the old `v2.9` remote tag.

Status: completed in `docs/v3.2-remote-tag-and-data-origin-scan.md`.

Decision: `REMOTE_TAG_SHOULD_BE_RECREATED_TO_SANITIZED_COMMIT`.

Summary:

- Remote `main` points to `d7f4b47ce308fc5faa10f78f1b69ef56324fa19e`.
- Remote `main` history has 4 sanitized public commits.
- Remote `v2.9` is an annotated tag whose peeled commit target is `91dc8e243d7600fe663824179b48a48877665ec7`.
- Remote `main` data-origin disclosure scan passed.
- Remote `main` artifact scan passed.
- Remote `main` README encoding and mojibake scan passed.
- Old remote `v2.9` tag keeps the previous one-commit snapshot reachable.
- Old remote `v2.9` tag exposes a DB artifact and old snapshot encoding issues.
- Recommended cleanup is to delete remote `v2.9`, then recreate it at sanitized public history commit `88d88ff81c2eff3db4eada0621b68b6de2b0be85`.
- No remote tag deletion, tag recreation, or GitHub push was performed in this step.

## Step 17

### Remote v2.9 Tag Cleanup Execution

Execute the approved remote `v2.9` tag cleanup from the upload execution folder only.

Status: completed outside the original repo.

Decision: `REMOTE_TAG_CLEANUP_PASS_PUBLIC_REFS_DISCLOSURE_CLEAN`.

Summary:

- The user explicitly approved deleting remote `v2.9` and recreating it on the sanitized v2.9 public-history commit.
- Old remote `v2.9` tag was deleted.
- Remote `v2.9` was recreated as a lightweight tag at `88d88ff81c2eff3db4eada0621b68b6de2b0be85`.
- Remote `main` remained `d7f4b47ce308fc5faa10f78f1b69ef56324fa19e`.
- Fresh public refs scan confirmed the previous one-commit snapshot is no longer reachable.
- Public refs disclosure blockers: `0`.
- Reachable bundled data artifacts: `0`.
- Privacy/path blockers: `0`.
- Actual secret/API key/token blockers: `0`.
- GitHub README BOM and mojibake checks passed.
- Original repo remote remained absent.

Known issue:

- Reachable history still contains non-README UTF-8 BOM occurrences. This is a public-quality cleanup candidate, not a privacy/data/artifact blocker.

## Step 18

### Remote Refs Cleanup Result Docs-only

Record the Step 17 remote refs cleanup result in original repo docs without mutating GitHub refs.

Status: completed in `docs/v3.2-remote-refs-cleanup-result.md`.

Decision: `REMOTE_REFS_CLEANUP_RESULT_DOCUMENTED_NO_REMOTE_CHANGE`.

Summary:

- Final public refs are documented.
- Public refs disclosure-clean verdict is documented.
- External/local data artifacts are documented as not included in public refs.
- The previous one-commit snapshot is documented as unreachable from fresh public remote refs.
- GitHub README BOM/mojibake pass state is documented.
- Non-README UTF-8 BOM occurrences are recorded as a known public-quality cleanup candidate.
- No GitHub push, remote tag change, force push, visibility change, or original repo remote addition was performed in this docs-only step.

## Step 19

### Public GitHub Repo Final Verification

Perform final read-only verification of the public GitHub repository after upload and remote tag cleanup.

Status: completed in `docs/v3.2-public-github-final-verification.md`.

Decision: `PUBLIC_GITHUB_FINAL_VERIFICATION_PASS_WITH_QUALITY_NOTE`.

Summary:

- GitHub repo visibility remains public.
- Public `main` is `d7f4b47ce308fc5faa10f78f1b69ef56324fa19e`.
- Public `v2.9` tag is `88d88ff81c2eff3db4eada0621b68b6de2b0be85`.
- The previous one-commit snapshot is not reachable from fresh public remote refs.
- Public refs disclosure blockers: `0`.
- Public refs privacy/path blockers: `0`.
- Actual secret/API key/token blockers: `0`.
- Bundled data, raw archive, generated report, build/cache, and test output artifacts: `0`.
- GitHub README has no BOM, no mojibake, and readable Korean.
- UTF-8 invalid text files: `0`.
- Root `LICENSE` is present at public `main`.
- Non-README UTF-8 BOM occurrences remain a public-quality cleanup candidate, not a privacy/data/artifact blocker.
- No GitHub push, remote tag change, force push, visibility change, original repo remote addition, or code/package/runtime mutation was performed in this step.

## Step 20

### v3.2 Release Wrap-up Docs-only

Summarize v3.2 completion after public GitHub upload, remote tag cleanup, and final verification.

Status: completed in `docs/v3.2-release-wrap-up.md`.

Decision: `V3_2_PUBLIC_WRAP_UP_COMPLETE`.

Summary:

- Public GitHub repo is documented as using sanitized 4-commit public history.
- Public `main` is `d7f4b47ce308fc5faa10f78f1b69ef56324fa19e`.
- Public `v2.9` tag is `88d88ff81c2eff3db4eada0621b68b6de2b0be85`.
- The previous public snapshot is documented as unreachable from fresh public remote refs.
- GitHub README BOM/mojibake issue is documented as resolved for public `main`.
- Public refs disclosure blockers remain `0`.
- Secret/privacy/path blockers remain `0`.
- Bundled data, raw archive, generated report, build/cache, and test output artifacts remain `0`.
- Original repo remote remains absent.
- Original repo DB SHA baseline remains unchanged.
- Public wording policy is documented with neutral data wording only.
- Non-README UTF-8 BOM occurrences remain a public-quality cleanup candidate, not a privacy/data/artifact blocker.
- No GitHub push, remote tag change, force push, visibility change, original repo remote addition, or package/API/UI/runtime mutation was performed in this docs-only step.

## Next Step Candidates

- Non-README BOM cleanup/public quality review.
- v3.2 stable tag planning.
- Public repository maintenance checklist.
- Public README/product polish.
- Codex/OpenAI application package review.
- Copied DB command entrypoint design refinement.
- Copied DB command entrypoint implementation without package script.
- Package script approval gate design docs-only.
- Report JSON write policy design docs-only.
- Restore-test copied DB write rehearsal readiness review.

## Post-CI Public Final Plan

### Public GitHub Final Verification After CI Fix

Re-verify public GitHub after the CI Node.js 24 fix and document the v3.2 tag and
remaining cleanup decisions.

Status: completed in `docs/v3.2-public-github-post-ci-final-plan.md`.

Decision: `PUBLIC_GITHUB_POST_CI_FINAL_VERIFICATION_PASS_WITH_QUALITY_NOTE`.

Summary:

- Public `main` is `f5d76709fd7f72541fc82896282fd9da1a653125`.
- Public `main` commit count is `6`.
- Public `v2.9` remains `88d88ff81c2eff3db4eada0621b68b6de2b0be85`.
- Latest GitHub Actions run after the CI Node.js 24 fix passed.
- README confirms Node.js 24 or newer and passes UTF-8/BOM/mojibake checks.
- CI verify and smoke jobs both use Node.js 24.
- Public docs required for OSS/Codex application review are present on public `main`.
- Fresh public refs scan found no disclosure, secret, strict privacy/path, bundled data, raw,
  generated, build, or test artifact blockers.
- The previous unsafe public snapshot remains unreachable.
- Remaining local backup-root label occurrences in old planning history are recorded as a
  known public-quality note, not an immediate rewrite blocker.
- Recommended `v3.2` public tag target is the pure sanitized v3.2 snapshot
  `d7f4b47ce308fc5faa10f78f1b69ef56324fa19e`.
- Current application-ready public `main` remains
  `f5d76709fd7f72541fc82896282fd9da1a653125`.
- Existing npm audit warnings should be triaged in a separate dependency-safety step.
- No GitHub push, tag change, force push, visibility change, original repo remote addition,
  dependency change, package/API/UI/runtime mutation, or DB write was performed in this
  docs-only step.

Next recommended actions:

- Create public `v3.2` tag with explicit user approval.
- Triage npm audit warnings in a separate step.
- Decide whether optional public-history quality cleanup is worth a force-rewrite flow.

## Public v3.2 Tag Final Verification

### Public Tag Verification Docs-only

Verify the public `v3.2` tag after creation and document the final public refs.

Status: completed in `docs/v3.2-public-tag-final-verification.md`.

Decision: `PUBLIC_V3_2_TAG_FINAL_VERIFICATION_PASS_WITH_QUALITY_NOTE`.

Summary:

- Public `main` remains `f5d76709fd7f72541fc82896282fd9da1a653125`.
- Public `v2.9` remains `88d88ff81c2eff3db4eada0621b68b6de2b0be85`.
- Public `v3.2` is `d7f4b47ce308fc5faa10f78f1b69ef56324fa19e`.
- Public `v3.2` is a lightweight tag on the pure v3.2 sanitized snapshot.
- Public `main` is the latest application-ready state after OSS/Codex application materials
  and the CI Node.js 24 fix.
- Fresh public refs scan found no disclosure, secret, strict privacy/path, bundled data,
  raw, generated, build, or test artifact blockers.
- README BOM/mojibake checks passed.
- Latest GitHub Actions run remains PASS.
- The previous unsafe public snapshot remains unreachable.
- Older planning-history local backup-root labels remain a known public-quality note.
- Existing npm audit warnings remain a separate dependency-safety triage candidate.
- No GitHub push, tag change, force push, visibility change, original repo remote addition,
  dependency change, package/API/UI/runtime mutation, or DB write was performed in this
  docs-only verification step.

## Public Repository Maintenance Checklist

### OSS/Codex Application Maintenance Checklist

Document the current public GitHub repository readiness and remaining non-blocking
maintenance items.

Status: completed in `docs/PUBLIC_REPOSITORY_MAINTENANCE_CHECKLIST.md`.

Decision: `APPLICATION_REPOSITORY_READY_WITH_NON_BLOCKING_NOTES`.

Summary:

- Public refs are recorded:
  - `main`: `f5d76709fd7f72541fc82896282fd9da1a653125`
  - `v2.9`: `88d88ff81c2eff3db4eada0621b68b6de2b0be85`
  - `v3.2`: `d7f4b47ce308fc5faa10f78f1b69ef56324fa19e`
- README, CONTRIBUTING, SECURITY, RELEASE_HISTORY, CI, and Node.js 24+ requirement are
  documented as present.
- Typecheck, tests, build, smoke, and latest GitHub Actions state are documented as passing.
- Public-safe blockers and bundled DB/raw/generated artifacts remain `0`.
- Safety scope remains off-table only with no live assistance, OCR, overlays, hotkeys,
  client integration, or bundled data artifacts.
- Known non-blocking issues remain:
  - npm audit warnings: `TRIAGE_RECOMMENDED_SEPARATE_STEP`
  - older planning-history local backup-root labels: `KEEP_AS_KNOWN_QUALITY_NOTE`
  - earlier failed GitHub Actions run remains visible in workflow history
- No GitHub push, tag change, force push, original repo remote addition, dependency change,
  package/API/UI/runtime mutation, or DB write was performed in this docs-only step.
