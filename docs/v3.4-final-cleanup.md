# v3.4 final cleanup

## Scope

This document records the final v3.4 cleanup batch.

The batch covers dependency maintenance, BOM cleanup, generated artifact hygiene, and final release-readiness review.

## Baseline

| Item | Result |
| --- | --- |
| Starting HEAD | `e294543d0e67dcdd90103c05f9265c0c2c6469e7` |
| Previous smoke commit | `e294543d0e67dcdd90103c05f9265c0c2c6469e7` |
| Package script | `copied-db:rehearsal:dry-run` |

## Dependency maintenance

| Item | Result |
| --- | --- |
| Initial audit warnings | 3 total: low `esbuild`, high `vite`, critical `vitest` |
| Packages reviewed | `esbuild`, `vite`, `vitest`, `tsx` transitive graph |
| Package changes | `vite` `^7.0.0` to `^7.3.6`; `vitest` `^3.2.0` to `^3.2.7` |
| package-lock changed | Yes, dev tooling lockfile entries updated |
| Transitive update | `esbuild` resolved to `0.28.1` |
| Final audit result | 0 vulnerabilities |
| Verdict | `AUDIT_CLEAN` |

## BOM cleanup

| Item | Result |
| --- | --- |
| Target | `apps/web/src/App.tsx` |
| BOM before | Present |
| BOM after | Absent |
| Verdict | `BOM_CLEANED` |

## Artifact hygiene

| Item | Result |
| --- | --- |
| Target files | `artifacts/latest-*.json` |
| Action | Removed tracked generated latest report pointers from Git; local ignored copies may remain for original-repo verification |
| `.gitignore` updated | No, existing `artifacts/` ignore rule already covers local generated reports |
| Verdict | `ARTIFACT_HYGIENE_CLEANED` |

## Safety verification

| Check | Result |
| --- | --- |
| Off-table-only scope preserved | PASS |
| No RTA/live assistance | PASS |
| No OCR/screen capture/overlay/hotkey/live watcher | PASS |
| No poker client integration | PASS |
| No DB write | PASS |
| No raw DB/raw zip access added | PASS |
| No API/UI/runtime integration added | PASS |
| Package script remains dry-run-only | PASS |
| Safe dry-run smoke | PASS, `DRY_RUN_PASS` |
| Missing dry-run fail-closed | PASS, `DRY_RUN_BLOCKED`, non-zero exit |

## Verification results

| Command | Result |
| --- | --- |
| `npm audit --json` | PASS, 0 vulnerabilities |
| `npm.cmd run typecheck` | PASS |
| `npm.cmd test` | PASS, core 383 / server 82 / web 104 |
| `npm.cmd run build` | PASS |
| safe dry-run command | PASS, `DRY_RUN_PASS`, exit `0` |
| missing dry-run command | PASS, `DRY_RUN_BLOCKED`, non-zero exit |

## Final verdict

`V3_4_FINAL_CLEANUP_READY`

## Remaining known issues

None currently blocking v3.4 release planning.

Operational notes:

- The existing test suite still emits Node SQLite experimental warnings in some tests.
- Build/test generated outputs must remain untracked and are cleaned after verification.
- Local ignored `artifacts/latest-*.json` files may remain in the original repo working tree for compatibility checks, but they are removed from Git tracking and are not part of the commit.

## Recommended next work

- v3.4 release/tag planning review
- v3.5 trainer UX planning
