# v3.5 public upload preparation

## Scope

This document records the local application and verification of the public-safe v3.5 snapshot in the public upload folder.

No GitHub push, tag push, public `v3.5` tag creation, force push, original repository remote addition, original history copy, DB copy, raw data copy, or generated report copy was performed.

## Baseline

| Item | Result |
| --- | --- |
| Public folder | `<local-backup-root>/poker-tournament-lab-public-upload-v3.5-20260708-20260709-181253` |
| Starting public HEAD | `7c296d921a3730ae81f650f515b9cc6dbb8ff21c` |
| Public branch | `main` |
| Public remote | `https://github.com/eojno9/poker-tournament-lab.git` |
| Remote main before push | `c6005f067f4a38902b885fe065ec2d3e769897b7` |
| Remote `v3.5` tag | Absent |
| Original local tag | `v3.5` |
| Original local tag target | `eaaaa3413a313fa0d2476d446b065958a1b52529` |
| Snapshot source | Original `v3.5` tag snapshot exported with `git archive` |

## Snapshot Application

The public upload folder was updated from the original `v3.5` tag snapshot using an explicit mirror copy plan.

The copy excluded:

- `.git`
- `node_modules`
- `dist`
- `coverage`
- `test-results`
- `playwright-report`
- `artifacts`
- `.env`
- `*.db`
- `*.sqlite`
- `*.sqlite3`
- `*.db-wal`
- `*.db-shm`
- `*.zip`
- `hrc-dry-run-*.json`
- internal public-upload/security scan planning documents that contain scan-target examples

`robocopy` completed with exit code `3`, which is within the accepted success range.

## Artifact And Privacy Check

| Check | Result |
| --- | --- |
| Snapshot artifact directory | None |
| DB files in snapshot | 0 |
| DB sidecar files in snapshot | 0 |
| zip files in snapshot | 0 |
| env files in snapshot | 0 |
| generated dry-run JSON in snapshot | 0 |
| public-folder strict local user/path scan | PASS, 0 |
| public-folder private relay literal scan | PASS, 0 |
| tracked `artifacts/` | None |
| package.json/package-lock.json unexpected diff | None |

The public folder intentionally keeps neutral public wording. Bundled data artifacts are not included, and users configure their own local data.

## Korean-First UI Check

The public snapshot contains Korean-first Trainer UI copy, including:

- `Trainer 학습`
- `오프테이블`
- `오답 복습`
- `학습 요약`
- `정답입니다.`
- `오답입니다.`
- local-only storage copy

## Safety Check

The safety scan found only explicit negative safety copy, such as no real-time assistance and no screen capture, OCR, overlay, hotkey, watcher, or poker-client integration.

No RTA/live assistance workflow, OCR, screen capture, overlay, hotkey workflow, live watcher, or poker client integration was added.

## Local Verification

| Check | Result |
| --- | --- |
| `npm.cmd ci` | PASS, 0 vulnerabilities |
| `npm.cmd run typecheck` | PASS |
| `npm.cmd test` | PASS, core 383 / server 82 / web 109 |
| `npm.cmd run build` | PASS |
| `npm.cmd run test:smoke` | PASS, 7/7 |
| Generated build/smoke outputs committed | None |

Build and smoke produced ignored local output folders. They were removed by explicit path cleanup and are not part of the commit.

## Public Upload Status

This step prepares and verifies the public upload folder locally only.

GitHub main and remote tags were not changed in this step.

## Final Verdict

`V3_5_PUBLIC_SNAPSHOT_APPLIED_READY`
