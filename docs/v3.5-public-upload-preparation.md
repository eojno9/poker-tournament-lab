# v3.5 public upload preparation dry-run

## Scope

This document records a dry-run preparation step for a future v3.5 public sanitized upload.

No GitHub push, tag push, force push, public overwrite, original history copy, DB copy, raw data copy, generated report copy, or dependency change was performed.

## Baseline

| Item | Result |
| --- | --- |
| Public folder | `<local-backup-root>/poker-tournament-lab-public-upload-v3.5-20260708-20260709-181253` |
| Public starting HEAD | `c6005f067f4a38902b885fe065ec2d3e769897b7` |
| Public current HEAD before this doc commit | `c6005f067f4a38902b885fe065ec2d3e769897b7` |
| Public branch | `main` |
| Public remote | `https://github.com/eojno9/poker-tournament-lab.git` |
| Remote main | `c6005f067f4a38902b885fe065ec2d3e769897b7` |
| Remote `v3.5` tag | Absent |
| Original local tag | `v3.5` |
| Original local tag target | `eaaaa3413a313fa0d2476d446b065958a1b52529` |
| Dry-run copy source | Original `v3.5` tag snapshot exported with `git archive` |

## Dry-Run Source Preparation

The original `v3.5` tag snapshot was exported into a temporary folder under the local backup root.

The archive was extracted from the tag snapshot only. The original repository `.git` history was not copied into the public folder.

## Excluded Paths And Patterns

The dry-run copy plan excludes:

- `.git`
- `node_modules`
- `dist`
- `coverage`
- `test-results`
- `playwright-report`
- `artifacts`
- `.env`
- `*.db`
- `*.sqlite`
- `*.sqlite3`
- `*.db-wal`
- `*.db-shm`
- `*.zip`
- `hrc-dry-run-*.json`

## Raw And Generated Artifact Check

| Check | Result |
| --- | --- |
| Tracked `artifacts/` in original repo | None |
| Snapshot artifact directory | None |
| DB files in snapshot | 0 |
| DB sidecar files in snapshot | 0 |
| zip files in snapshot | 0 |
| env files in snapshot | 0 |
| generated dry-run JSON in snapshot | 0 |
| `node_modules` in snapshot | None |
| `dist` in snapshot | None |
| `coverage` in snapshot | None |
| `test-results` in snapshot | None |
| `playwright-report` in snapshot | None |

## Robocopy List-Only Result

`robocopy` was run in list-only mode with `/L`.

Summary:

- files that would be copied or refreshed: 259
- bytes that would be copied or refreshed: about 2.26 MB
- failed items: 0
- extra public-folder files observed by list-only comparison: 3
- actual file copy: none

The observed extra files were not deleted or modified in this dry-run. The next upload step should decide whether the public folder should be synchronized by explicit copy rules, a clean regenerated folder, or a reviewed public-safe replacement plan.

## Safety Result

| Constraint | Result |
| --- | --- |
| GitHub push | None |
| Tag push | None |
| Force push | None |
| Original repo remote addition | None |
| Original history copy | None |
| Public snapshot overwrite | None |
| DB/raw/generated artifact copy | None |
| package/dependency change | None |

## Public Upload Readiness

The dry-run preparation is ready for the next step. The next step should perform an explicit public-safe snapshot application plan before any GitHub push or tag push.

## Final Verdict

`V3_5_PUBLIC_UPLOAD_DRY_RUN_READY`
