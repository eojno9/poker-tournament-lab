# v3.4 GitHub upload preflight

## Scope

This document records the GitHub upload preflight review for v3.4.

No GitHub push is performed in this step.
No tag push is performed in this step.
No original repo remote is added in this step.
No local tag is created in this review step unless separately approved.

## Baseline

| Item | Result |
| --- | --- |
| Starting HEAD | `684551a7123922c3330df3cd1e45808e858224cb` |
| Candidate tag | `v3.4` |
| Candidate release title | `v3.4 - Dry-run copied DB rehearsal skeleton` |
| Original repo remote | Absent |
| Existing local `v3.4` tag | Absent |
| Tag points at HEAD | No local `v3.4` tag exists |

## Verification summary

| Check | Result |
| --- | --- |
| npm audit | PASS, 0 vulnerabilities |
| typecheck | PASS |
| test | PASS, core 383 / server 82 / web 104 |
| build | PASS |
| safe dry-run smoke | PASS, `DRY_RUN_PASS`, exit `0` |
| missing dry-run fail-closed | PASS, `DRY_RUN_BLOCKED`, non-zero exit |
| tracked artifacts | PASS, `git ls-files artifacts` returned no tracked files |
| generated artifacts committed | None |
| DB/raw access | No new DB/raw access; package script path does not access DB/raw files |
| API/UI/runtime connection | None added |
| GitHub push | None |
| Tag push | None |
| Original repo remote added | No |

## Public upload safety review

| Area | Result |
| --- | --- |
| Public-safe wording | PASS |
| No bundled DB/raw/generated artifacts | PASS |
| No data source/provider/permission/private path disclosure | PASS |
| Off-table-only scope | PASS |
| No RTA/live assistance | PASS |
| No OCR/screen capture/overlay/hotkey/live watcher | PASS |
| No poker client integration | PASS |
| Package script remains dry-run-only | PASS |

## Local tag readiness

Candidate local tag command, not executed in this review step:

```bash
git tag -a v3.4 -m "v3.4 - Dry-run copied DB rehearsal skeleton"
```

Local tag should only be created after explicit approval.

## GitHub upload readiness

GitHub upload must not be performed from the original repo unless the public-safe upload path has been explicitly reviewed.

Do not add a remote to the original repo.

Recommended next step:

- prepare a separate public-safe upload workflow
- confirm sanitized public repo target
- confirm branch/tag push commands
- execute only after explicit approval

## Final verdict

`GITHUB_UPLOAD_PREFLIGHT_READY`

## Recommended next action

Create local `v3.4` tag in a separately approved step.
