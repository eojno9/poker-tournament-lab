# v3.2 Public GitHub Post-CI Final Plan

## Purpose

This document records the read-only public GitHub verification after the CI Node
version fix, then plans the v3.2 public tag boundary and remaining public-history
cleanup decisions.

This step did not push to GitHub, create or move tags, change GitHub visibility, add a
remote to the original repo, rewrite history, change dependencies, or mutate runtime code.

## Public GitHub State After CI Fix

Verdict: `PUBLIC_GITHUB_POST_CI_FINAL_VERIFICATION_PASS_WITH_QUALITY_NOTE`.

- Repository visibility: public.
- Public `main`: `f5d76709fd7f72541fc82896282fd9da1a653125`.
- Public commit count on `main`: `6`.
- Public `v2.9` tag: `88d88ff81c2eff3db4eada0621b68b6de2b0be85`.
- Previous unsafe public snapshot: unreachable from fresh public refs.
- Latest GitHub Actions run: PASS.
- CI workflow now uses Node.js 24 in both verify and smoke jobs.
- README prerequisite now says Node.js 24 or newer.
- README UTF-8/BOM/mojibake checks passed.
- `CONTRIBUTING.md`, `SECURITY.md`, and `docs/RELEASE_HISTORY.md` are present on public `main`.
- No bundled data artifacts are included.

## Public Refs Scan Summary

Fresh public refs were scanned from a repo-external mirror clone.

- Disclosure blocker: `0`.
- Actual secret/API key/token blocker: `0`.
- Strict privacy/path blocker: `0`.
- Bundled DB/raw/generated/build/test artifact objects: `0`.
- Old unsafe snapshot reachable: no.
- `main` and `v2.9` point to the expected sanitized refs.

The scan also found a remaining public-history quality note: older v2.x planning
documents in the first sanitized public snapshot contain a local backup-root label in
13 lines. This is not a bundled data artifact, not a user-account path, not a data
source/provider/permission disclosure, and not introduced by the OSS/Codex prep or CI
fix commits.

## v3.2 Position

The original local repo contains the full v3.2 Step 1-20 planning, verification, and
public-release-preparation record.

The public GitHub repo intentionally contains sanitized public history only. Its current
`main` includes:

1. `v2.9` sanitized stable source snapshot.
2. `v3.0` copied DB import foundation snapshot.
3. `v3.1` copied DB rehearsal planning foundation.
4. Pure v3.2 sanitized clean snapshot.
5. OSS/Codex application material update.
6. CI Node.js 24 fix.

The pure v3.2 sanitized snapshot is:

`d7f4b47ce308fc5faa10f78f1b69ef56324fa19e`

The current application-ready public `main` is:

`f5d76709fd7f72541fc82896282fd9da1a653125`

## v3.2 Tag Strategy

Decision: `RECOMMEND_TAG_V3_2_AT_PURE_SANITIZED_SNAPSHOT`.

Recommended target:

`d7f4b47ce308fc5faa10f78f1b69ef56324fa19e`

Rationale:

- It preserves the clean v3.2 snapshot boundary.
- It keeps later OSS/Codex application polish and CI maintenance as post-v3.2 `main`
  improvements.
- It avoids mixing the v3.2 release boundary with application-packaging work.

Alternative:

- Tag `f5d76709fd7f72541fc82896282fd9da1a653125` if the desired meaning of `v3.2` is
  "application-ready public main after CI fix."
- This is not recommended because it blurs the release boundary.

No tag was created in this step.

## Remaining Public-History Cleanup Decision

Decision: `KEEP_AS_KNOWN_QUALITY_NOTE`.

Reasoning:

- The remaining hits are local backup-root labels in old planning documents.
- They do not expose bundled data, actual user-account paths, private data source details,
  provider details, permission details, secrets, or generated data artifacts.
- Removing them from all public history would require rebuilding sanitized public history and
  force-pushing, which should be reserved for a true blocker or an explicit public-history
  cleanup decision.

If the project wants a zero-quality-note public history later, use a separate force-rewrite
planning step and require explicit approval before any destructive remote update.

## npm Audit Warning Decision

Decision: `TRIAGE_RECOMMENDED_SEPARATE_STEP`.

Current status:

- `npm ci` passes.
- Typecheck, tests, build, smoke, and GitHub Actions pass after the CI Node.js 24 fix.
- Existing audit warnings are not treated as a release-blocking functional failure in this step.

Dependency changes remain out of scope here and should be handled in a separate dependency
triage step.

## Required Approval Before Future Actions

For public tag creation:

`public GitHub에 v3.2 tag를 생성해도 좋다.`

For any future public-history rewrite:

`public GitHub history를 sanitized history로 다시 덮어써도 좋다.`

## Next Step Candidates

- Public `v3.2` tag creation on the pure sanitized snapshot.
- npm audit warning triage in a separate dependency-safety step.
- Optional public-history quality cleanup planning.
- Public GitHub repository maintenance checklist.
